# turbowarp_project

`go2_teleop.sb3` is the TurboWarp project used to send velocity commands over WebSocket.

## Use
1. Open TurboWarp editor.
2. Load `go2_teleop.sb3`.
3. Set the connection URL to your bridge:
- Same laptop: `ws://127.0.0.1:8765`
- Another device: `ws://<LAPTOP_IP>:8765`
